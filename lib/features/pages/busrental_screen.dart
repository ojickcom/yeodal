import 'package:flutter/material.dart';

class BusrentalScreen extends StatelessWidget {
  const BusrentalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("버스렌탈 서비스"),
      ),
    );
  }
}
